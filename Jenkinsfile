node("Built-In Node") {
        // This is a demo
    try {
            stage("Checking out SCM") {
        // try{
            checkout scm
        // }
        
    }
    }
   catch (Execption e) {
             echo 'Exception occurred dureing Checkout: ' + e.toString()
             sh 'Handle the exception!'
        }  

}
